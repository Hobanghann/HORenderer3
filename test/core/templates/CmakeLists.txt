################################################################################
# Read source files
################################################################################
file(GLOB_RECURSE SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/*.cc)
file(GLOB_RECURSE HEADERS ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

set(ALL_FILES ${SOURCES} ${HEADERS})

################################################################################
# Add excutable target
################################################################################
include(GoogleTest)
add_executable(${TEMPLATES_TEST_NAME} ${ALL_FILES})

################################################################################
# Set target properteis
################################################################################
target_include_directories(
  ${TEMPLATES_TEST_NAME}
  PUBLIC
  ${CMAKE_CURRENT_SOURCE_DIR}
)
target_link_libraries(${TEMPLATES_TEST_NAME} PRIVATE ${THIRD_PARTY_NAME} ${CORE_NAME} GTest::gtest_main)
set_target_properties(${TEMPLATES_TEST_NAME} PROPERTIES FOLDER "Test/core")

################################################################################
# Add test
################################################################################
add_test(NAME ${TEMPLATES_TEST_NAME} COMMAND $<TARGET_FILE:${TEMPLATES_TEST_NAME}>)
gtest_discover_tests(${TEMPLATES_TEST_NAME}
  DISCOVERY_TIMEOUT 30
  WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}
)